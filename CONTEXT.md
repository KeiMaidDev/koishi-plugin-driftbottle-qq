# 漂流瓶（driftbottle-qq）

Koishi 漂流瓶插件：用户扔瓶、捞瓶、留言、举报；管理员对瓶子进行事后管理。本文件是领域术语表。

## Language

### 瓶子

**瓶子（Bottle）**：
用户扔出的一条内容（文本 + 可选图片/音频），存于本地 LocalStorage。
_Avoid_: 投稿、帖子

**本地瓶（Local Bottle）**：
存放在本机 LocalStorage 的瓶子。控制台管理面板只管理本地瓶。
_Avoid_: 数据库瓶子

**云瓶（Cloud Bottle）**：
存放在远端漂流瓶服务器（webBottle 客户端所连服务）的瓶子，本插件无管理权限。

**留言（Review）**：
挂在某个瓶子下的评论，随瓶子存储，支持软删除（`isDel` 标记）。
_Avoid_: 评论、回复

### 状态与管理

**封禁（Ban）**：
把瓶子的 `show` 标记置为 false，使其不再对普通用户可见；可解封恢复。封禁不是删除。
_Avoid_: 删除（指封禁时）、屏蔽

**举报（Report）**：
用户对瓶子发起的投诉记录，同一用户去重；累计达举报阈值后通知管理员。
_Avoid_: 投诉

**举报阈值（Report Threshold）**：
触发管理员通知所需的举报人数下限（`reportThreshold`，默认 3）。阈值只决定 QQ 私聊催办，不影响网页上可见性。

**待处理举报（Pending Report）**：
举报数达到阈值且尚未被处理（封禁或忽略）的举报。

**忽略举报（Dismiss Report）**：
管理员判定举报不成立的处理动作：不封瓶，仅把该瓶的举报标记为已处理，停止重复通知。
_Avoid_: 驳回、误报标记

**管理员（Admin）**：
QQ 端指令由 `config.adminQQ` 白名单校验；控制台管理面板的权限是控制台登录本身，不要求在 adminQQ 内。
_Avoid_: OP、超级管理员

**审核**：
在本项目中特指事后管理（封禁/解封、删留言、处理举报），不是发布前的检查。
_Avoid_: 内容安全审核（指腾讯云自动过滤 `delSetu` 时）、预审（指入海前人工检查时）

### 预审

**预审（Pre-review）**：
管理员在瓶子入海前对其内容做的人工检查；仅当自动内容安全审核不可用时启用。
_Avoid_: 投稿审核、审核

**待审投稿（Pending Submission）**：
已收集但尚未通过预审的瓶子内容，存放在待审区：不入海、捞不到、不占用瓶子 ID。
_Avoid_: 待审瓶（通过预审前还不是瓶子）

**通过（Approve）**：
预审放行动作：待审投稿转正为瓶子（获得 ID）并入海。
_Avoid_: 解封（封禁的逆操作，针对已入海的瓶子）

**驳回（Reject）**：
预审拒绝动作：丢弃待审投稿及其媒体文件，不产生瓶子；可附带一条仅作者可见的驳回理由。
_Avoid_: 封禁（封禁针对已入海的瓶子且不删除）

**撤回（Withdraw）**：
作者在预审出结果前主动丢弃自己待审投稿的动作，效果与驳回一致；已入海的瓶子不存在撤回。
_Avoid_: 删除瓶子、取消投稿

**待审编号（Pending Number）**：
待审投稿的稳定引用编号：全局递增、持久化、不复用；管理员通知、通过、驳回、撤回都以它指代投稿。
_Avoid_: 瓶子 ID（通过预审前不存在瓶子 ID）
